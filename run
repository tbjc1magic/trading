#!/bin/bash

mkdir -p proto_generate/proto

python -m grpc_tools.protoc \
    -I./protos \
    --python_out=proto_generate/proto \
    --grpc_python_out=proto_generate/proto \
    protos/order_book/order_book_service.proto
